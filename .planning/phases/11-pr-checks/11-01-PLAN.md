# Phase 11 — Plan 01: PR Checks Implementation and Tests

**Goal:** `gf pr checks <number>` produces correct forge CLI args for GitHub, GitLab, Forgejo; returns `UnsupportedFeature` for Gitea.

## Must-Haves

- `checks` clap subcommand under `pr` with optional `number` positional and `extra` passthrough
- `translate_pr_checks()` function in `adapter/pr.rs` producing correct args per forge:
  - GitHub → `["pr", "checks", "<number>"]`
  - GitLab → `["ci", "status"]` (ignores number — glab ci status is branch-based)
  - Forgejo → `["pr", "status", "<number>"]`
  - Gitea → `Err(GfError::UnsupportedFeature { .. })`
- Match arm in `translate_pr()` dispatching `"checks"` to the new function
- 6 translation tests (GitHub/GitLab/Forgejo × with/without number)
- 1 unsupported test for Gitea
- 3 audit tests for `gh pr checks`, `glab ci status`, `fj pr status`
- All 381 existing tests still pass; zero compiler warnings

## Tasks

1. **Add checks clap subcommand and translation function** — Add `checks` to `build_pr()` in `cmd/mod.rs` following `view` shape. Implement `translate_pr_checks()` in `adapter/pr.rs`. Critical: GitLab must produce `["ci", "status"]`, NOT `["mr", "status"]`.
   - Files: `src/cmd/mod.rs`, `src/adapter/pr.rs`
   - Verify: `cargo build` zero warnings, `cargo run -- pr --help` shows `checks`

2. **Add translation, unsupported, and audit tests** — Add 10 macro-based tests to `tests/flag_audit.rs`.
   - Files: `tests/flag_audit.rs`
   - Verify: `cargo test pr_checks` passes, `cargo test` passes 391+ tests

## Files Touched

- `src/cmd/mod.rs` — Added `checks` subcommand (~12 lines)
- `src/adapter/pr.rs` — Added `translate_pr_checks()` (~50 lines) + match arm (1 line)
- `tests/flag_audit.rs` — Added 10 macro-based tests (~30 lines)
