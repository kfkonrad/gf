---
phase: 14-final-integration
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - tests/integration_test.rs
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: [PR-08, PR-09, ISSUE-07, ISSUE-08]

must_haves:
  truths:
    - "Integration tests verify all v1.2 commands via assert_cmd end-to-end"
    - "Help text for pr shows checks, comment, and edit subcommands"
    - "Help text for issue shows comment and edit subcommands"
    - "PROJECT.md reflects new command surface and final test count"
    - "ROADMAP.md shows Phase 14 complete and v1.2 milestone done"
    - "STATE.md shows milestone complete"
    - "Zero compiler warnings on cargo build"
    - "Full test suite green"
  artifacts:
    - path: "tests/integration_test.rs"
      provides: "Integration tests for all v1.2 help text and CLI behavior"
      contains: "test_pr_help_shows_checks"
    - path: ".planning/PROJECT.md"
      provides: "Updated command surface and test counts"
      contains: "PR CI status viewing"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 14 marked complete, v1.2 milestone complete"
      contains: "Phase 14"
    - path: ".planning/STATE.md"
      provides: "Updated state showing v1.2 complete"
      contains: "completed"
  key_links:
    - from: "tests/integration_test.rs"
      to: "src/cmd/mod.rs"
      via: "assert_cmd testing gf binary help output"
      pattern: "cargo_bin.*gf"
---

<objective>
Add integration tests for all v1.2 commands and update documentation to reflect milestone completion.

Purpose: Proves all v1.2 features work end-to-end through the `gf <command>` → clap → translate pipeline. Updates PROJECT.md command surface, ROADMAP.md, and STATE.md to reflect v1.2 completion.

Output: Integration tests for all v1.2 help text; updated documentation marking v1.2 as shipped.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-final-integration/14-01-SUMMARY.md
@.planning/phases/14-final-integration/14-RESEARCH.md

<interfaces>
<!-- Existing integration test pattern from tests/integration_test.rs -->
```rust
use assert_cmd::Command;
use predicates::prelude::*;

// Example existing test pattern:
#[test]
fn test_pr_help_text() {
    Command::cargo_bin("gf")
        .unwrap()
        .args(["pr", "--help"])
        .assert()
        .success()
        .stdout(predicate::str::contains("create"));
}
```

<!-- Helper functions available in integration_test.rs -->
```rust
fn setup_github_repo() -> (tempfile::TempDir, std::path::PathBuf) { ... }
fn make_git_only_bin_dir() -> (tempfile::TempDir, std::path::PathBuf) { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add integration tests for all v1.2 commands</name>
  <files>tests/integration_test.rs</files>
  <read_first>
    - tests/integration_test.rs (see existing test patterns, helpers, import style)
    - src/cmd/mod.rs (verify help text strings for checks, comment, edit subcommands)
    - .planning/phases/14-final-integration/14-RESEARCH.md (integration test patterns, lines 116-165)
  </read_first>
  <action>
Add 10 integration tests to `tests/integration_test.rs` that verify v1.2 command help text and CLI behavior using `assert_cmd`. Add these at the END of the file, in a clearly marked section.

The tests follow the existing pattern: `Command::cargo_bin("gf").unwrap().args([...]).assert().success().stdout(predicate::str::contains("..."));`

Add this block at the end of the file:

```rust
// ═══════════════════════════════════════════════════════════════════════════════
// v1.2 Integration Tests — Help text and CLI behavior verification
// ═══════════════════════════════════════════════════════════════════════════════

// --- PR help shows all v1.2 subcommands ---

#[test]
fn test_v12_pr_help_shows_checks() {
    Command::cargo_bin("gf")
        .unwrap()
        .args(["pr", "--help"])
        .assert()
        .success()
        .stdout(predicate::str::contains("checks"));
}

#[test]
fn test_v12_pr_help_shows_comment() {
    Command::cargo_bin("gf")
        .unwrap()
        .args(["pr", "--help"])
        .assert()
        .success()
        .stdout(predicate::str::contains("comment"));
}

#[test]
fn test_v12_pr_help_shows_edit() {
    Command::cargo_bin("gf")
        .unwrap()
        .args(["pr", "--help"])
        .assert()
        .success()
        .stdout(predicate::str::contains("edit"));
}

// --- Issue help shows all v1.2 subcommands ---

#[test]
fn test_v12_issue_help_shows_comment() {
    Command::cargo_bin("gf")
        .unwrap()
        .args(["issue", "--help"])
        .assert()
        .success()
        .stdout(predicate::str::contains("comment"));
}

#[test]
fn test_v12_issue_help_shows_edit() {
    Command::cargo_bin("gf")
        .unwrap()
        .args(["issue", "--help"])
        .assert()
        .success()
        .stdout(predicate::str::contains("edit"));
}

// --- Subcommand help text verification ---

#[test]
fn test_v12_pr_checks_help() {
    Command::cargo_bin("gf")
        .unwrap()
        .args(["pr", "checks", "--help"])
        .assert()
        .success()
        .stdout(predicate::str::contains("CI/check status"));
}

#[test]
fn test_v12_pr_comment_help() {
    Command::cargo_bin("gf")
        .unwrap()
        .args(["pr", "comment", "--help"])
        .assert()
        .success()
        .stdout(predicate::str::contains("Add a comment to a pull request"));
}

#[test]
fn test_v12_issue_comment_help() {
    Command::cargo_bin("gf")
        .unwrap()
        .args(["issue", "comment", "--help"])
        .assert()
        .success()
        .stdout(predicate::str::contains("Add a comment to an issue"));
}

#[test]
fn test_v12_pr_edit_help() {
    Command::cargo_bin("gf")
        .unwrap()
        .args(["pr", "edit", "--help"])
        .assert()
        .success()
        .stdout(predicate::str::contains("Edit a pull request"));
}

#[test]
fn test_v12_issue_edit_help() {
    Command::cargo_bin("gf")
        .unwrap()
        .args(["issue", "edit", "--help"])
        .assert()
        .success()
        .stdout(predicate::str::contains("Edit an issue"));
}
```

After adding, run `cargo test --test integration_test` to verify all integration tests pass.
  </action>
  <verify>
    <automated>cd /Users/derkev/tmp/gf-v2 && cargo test --test integration_test 2>&1 | grep "test result:" && cargo test test_v12 2>&1 | grep -c "test .* ok"</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --test integration_test 2>&1 | grep "test result:"` shows 0 failures
    - `grep -c "fn test_v12" tests/integration_test.rs` returns 10
    - `cargo test test_v12_pr_help_shows_checks 2>&1` shows 1 test passing
    - `cargo test test_v12_pr_help_shows_comment 2>&1` shows 1 test passing
    - `cargo test test_v12_issue_help_shows_comment 2>&1` shows 1 test passing
    - `cargo test test_v12_pr_checks_help 2>&1` shows 1 test passing
    - `cargo test test_v12_issue_comment_help 2>&1` shows 1 test passing
    - `cargo test test_v12_pr_edit_help 2>&1` shows 1 test passing
    - `cargo test test_v12_issue_edit_help 2>&1` shows 1 test passing
  </acceptance_criteria>
  <done>
    10 new integration tests added and passing. All verify v1.2 command help text end-to-end via assert_cmd.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update PROJECT.md, ROADMAP.md, and STATE.md for v1.2 completion</name>
  <files>.planning/PROJECT.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - .planning/PROJECT.md (current command surface, test counts, requirements section)
    - .planning/ROADMAP.md (current phase status table, v1.2 section)
    - .planning/STATE.md (current position and progress)
  </read_first>
  <action>
Update all three documentation files to reflect v1.2 milestone completion. Run `cargo test 2>&1` first to get the exact final test count, then use that number in documentation.

**File 1: .planning/PROJECT.md**

1. In the `### Active` requirements section, move all 4 items to `### Validated`:
   - Move `PR CI status viewing (PR-08) — v1.2` to Validated, prefix with `- ✓`
   - Move `Add/remove reviewers on PRs (PR-09) — v1.2` to Validated, prefix with `- ✓`
   - Move `Comment on issues (ISSUE-07) — v1.2` to Validated, prefix with `- ✓`
   - Move `Assign/remove labels on issues (ISSUE-08) — v1.2` to Validated, prefix with `- ✓`
   The `### Active` section should be empty or removed.

2. In the `## Context` section, update the line `Shipped v1.1 with 3,600 LOC Rust.` to reflect v1.2:
   Replace with: `Shipped v1.2 with ~4,000 LOC Rust. Tech stack: Rust, clap 4, webbrowser, toml, serde.`

3. Update the test count line (currently says "284 tests") to the actual count from `cargo test` output. The format should be: `{TOTAL} tests: ...` Include breakdown if known. Also add the new command categories: PR checks, issue/PR comments, PR/issue edit.

4. Update the `*Last updated:*` line to current date and `after v1.2 milestone completion`.

**File 2: .planning/ROADMAP.md**

1. Change Phase 14 line from `- [ ] Phase 14: Final Integration and Polish — pending (depends on 11, 12, 13)` to `- [x] Phase 14: Final Integration and Polish — completed`

2. In the progress table, update Phase 14 row:
   - Plans Complete: `2/2`
   - Status: `Complete`
   - Completed: current date

3. Change the v1.2 header from `### 🔄 v1.2 Workflow Completeness (Phases 11-14) — IN PROGRESS` to `### ✅ v1.2 Workflow Completeness (Phases 11-14) — SHIPPED {date}`

4. Add plan list to Phase 14 section:
```
**Plans:** 2/2 plans complete

Plans:
- [x] 14-01-PLAN.md — Restore lost Phase 11/12 code (checks + comment)
- [x] 14-02-PLAN.md — Integration tests and documentation update
```

**File 3: .planning/STATE.md**

1. Update frontmatter:
   - `status: completed`
   - `stopped_at: v1.2 milestone complete`
   - `last_updated:` to current ISO timestamp
   - `last_activity:` to current date with Phase 14 completion note
   - `progress.completed_phases: 4`
   - `progress.completed_plans: 5` (3 existing + 2 new)
   - `progress.percent: 100`

2. Update `## Current Position`:
   - Phase: 14 of 14 (Final Integration)
   - Plan: 2 of 2 (complete)
   - Status: milestone complete
   - Progress bar: `[██████████] 100%`

3. Update `**Current focus:**` to reflect v1.2 complete.

4. Add accumulated context entry: `- v1.2 shipped with ~4,000 LOC Rust, 14 phases, X plans, Y tests`
  </action>
  <verify>
    <automated>cd /Users/derkev/tmp/gf-v2 && cargo test 2>&1 | grep "test result:" && cargo build 2>&1 | grep -c "warning" && grep "PR CI status" .planning/PROJECT.md && grep "Phase 14.*complete\|Phase 14.*Complete" .planning/ROADMAP.md && grep "percent: 100" .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test 2>&1 | grep "test result:"` shows 0 failures across all binaries
    - `cargo build 2>&1 | grep -c "warning"` returns 0
    - `grep "✓ PR CI status viewing" .planning/PROJECT.md` matches
    - `grep "✓ Comment on issues" .planning/PROJECT.md` matches
    - `grep "✓ Add/remove reviewers" .planning/PROJECT.md` matches
    - `grep "✓ Assign/remove labels" .planning/PROJECT.md` matches
    - `grep "Active" .planning/PROJECT.md` section is empty or removed (no active requirements left)
    - `grep "percent: 100" .planning/STATE.md` matches
    - `grep "status: completed" .planning/STATE.md` matches
    - `grep -E "\[x\] Phase 14" .planning/ROADMAP.md` matches
    - `grep "SHIPPED" .planning/ROADMAP.md | grep "v1.2"` matches
  </acceptance_criteria>
  <done>
    PROJECT.md shows all 4 v1.2 requirements validated with updated test counts and command surface. ROADMAP.md marks Phase 14 complete and v1.2 shipped. STATE.md shows 100% progress and milestone complete. Full test suite green. Zero warnings.
  </done>
</task>

</tasks>

<verification>
1. `cargo test 2>&1` — all tests pass, 0 failures across all binaries
2. `cargo build 2>&1 | grep -c "warning"` — returns 0
3. `cargo test test_v12 2>&1` — all 10 new integration tests pass
4. Total test count is at least 469 (459 from plan 01 + 10 integration)
5. PROJECT.md has no Active requirements — all moved to Validated
6. ROADMAP.md shows Phase 14 complete and v1.2 SHIPPED
7. STATE.md shows 100% progress
</verification>

<success_criteria>
- 10 new integration tests verify all v1.2 commands end-to-end
- All tests pass (469+ total), zero compiler warnings
- PROJECT.md updated with v1.2 command surface and accurate test count
- ROADMAP.md marks v1.2 as shipped
- STATE.md shows milestone complete at 100%
</success_criteria>

<output>
After completion, create `.planning/phases/14-final-integration/14-02-SUMMARY.md`
</output>
